## How to set up a Python runtime environment for the Whole Cell Model.
## (See docs/create-pyenv.md for more details, esp. for Sherlock.)
## (See docs/dev-tools.md for prerequisites such as installing command line
## tools, additional SDK headers, and add pyenv init to your shell.)
#
## Install these packages using your local package manager such as homebrew on macOS:
#   brew install pyenv pyenv-virtualenv pyenv-virtualenvwrapper
#   brew install glpk openssl readline swig suite-sparse xz
#
## Whole Cell Model requires OpenBLAS 0.3.7+ or equivalent. Installing numpy and
## scipy binary wheels now provides a suitable OpenBLAS by default.
## (OpenBLAS before 0.3.7 had threading bugs. So does Apple's "Accelerate"
## framework. We haven't tested recent releases of Intel Math Kernel Library.)
#
## Install the required version of Python via pyenv, and remember to enable it as
## a shared library:
#   PYTHON_CONFIGURE_OPTS="--enable-shared" pyenv install 3.8.3
#
## Create the "wcEcoli3" python virtual environment based on that Python version,
## and select it for your project directory:
#   cd ~/dev/wcEcoli
#   pyenv virtualenv 3.8.3 wcEcoli3 && pyenv local wcEcoli3
#
## Upgrade this virtual environment's installers:
#   pip install --upgrade pip setuptools virtualenv virtualenvwrapper virtualenv-clone wheel
#
## Install numpy (if it's the first time) then all the packages listed in this file:
#   pip install numpy
#   pip install -r py3_requirements.txt && pyenv rehash
#
## Configure the installed matplotlib per the instructions in docs/create-pyenv.md

pip
setuptools
virtualenv
virtualenv-clone
virtualenvwrapper
wheel

numpy
scipy

biopython
bokeh==1.4.0
borealis-fireworks
confluent-kafka
cvxpy
Cython
Equation
FireWorks
future
ipdb
ipython
line-profiler
llvmlite
matplotlib
mock
numba
pandas
Pillow
pymongo
pytest
PyYAML
ruamel.yaml
six
stochastic-arrow
swiglpk
sympy
Theano
Unum
wholecell-vivarium
